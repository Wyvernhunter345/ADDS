#ifndef REVERSE
#define REVERSE
#include <string>

class Reverse
{
public:
    int reverseDigit(int n);                  // reverses a non-negative integer using recursion
    std::string reverseString(std::string s); // reverses a string using recursion
};

#endif // REVERSE